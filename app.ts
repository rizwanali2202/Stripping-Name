let personName : string = `\n\t rizwan ali \n\t`;
console.log(personName);
let trimmed : string = (personName.trim());
console.log(trimmed);

// \n is use for next line
// \t is use for four spaces like as tab
// trim kasi b function ke start ke spaces ko khatam karnay k laya use hota ha